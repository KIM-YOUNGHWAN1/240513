import React from "react";
import {FooterContainer} from "./styled";

export const Footer = () => {
  return (
    <FooterContainer>
      {/* <FooterContents onClick={handleMenuToggle}> TO-DO-LIST</FooterContents> */}
    </FooterContainer>
  );
};

export default Footer;
